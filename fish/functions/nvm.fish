function nvm
    echo "Obviously it's not the right language for you (or anyone). You should erase the JS from your system and pretend it never happened. 😉"
    bass source ~/.nvm/nvm.sh --no-use ';' nvm $argv
end
